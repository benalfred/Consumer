// export default async function({ route, redirect, $auth, $axios, store, dispatch }) {
//     console.log('running inside')
//     if ($auth.user && $auth.user.FirstName === null) {
//         redirect('/accountSetup1')
//     }
// }